 'use client';
import {useState} from 'react';import {Plus,Truck} from 'lucide-react';import {useAuth} from './AuthProvider';import {useData,rows,State,Modal,Row} from './DataUI';import AssetAssignmentModal from './AssetAssignmentModal';
function Assignment({assetId,projectId,onClose,onSaved}:{assetId:string;projectId:string;onClose:()=>void;onSaved:()=>void}){const data=useData('/api/v1/assets/'+assetId+'/overview');if(!data.data)return <Modal name="Loading asset assignment" onClose={onClose}><State loading={data.loading} error={data.error} retry={data.reload}><span/></State></Modal>;return <AssetAssignmentModal asset={data.data.asset} currentProject={data.data.current_project} projectId={projectId} onClose={onClose} onSaved={onSaved}/>;}
export default function ProjectAssetActions({projectId,onSaved}:{projectId:string;onSaved:()=>void}){const auth=useAuth();const [open,setOpen]=useState(false);const [asset,setAsset]=useState('');const [search,setSearch]=useState('');const data=useData(open?'/api/v1/assets?page_size=100&search='+encodeURIComponent(search):null);if(!auth.can('assets.read')||(!auth.can('assets.assign')&&!auth.can('assets.transfer')))return null;return <><button className="btn-primary text-xs" onClick={()=>setOpen(true)}><Plus size={13}/>Assign / transfer asset</button>{open&&<Modal name="Choose equipment for this project" onClose={()=>setOpen(false)}><input aria-label="Find asset" className="input-field mb-4" placeholder="Search equipment…" value={search} onChange={e=>setSearch(e.target.value)}/><State loading={data.loading} error={data.error} retry={data.reload}><div className="space-y-2">{rows(data.data).filter(r=>r.is_active&&r.current_project?.id!==projectId&&(r.current_project?auth.can('assets.transfer'):auth.can('assets.assign'))).map(r=><button className="w-full flex gap-3 items-center border rounded-lg p-3 hover:bg-muted text-left" key={r.id} onClick={()=>{setAsset(r.id);setOpen(false);}}><Truck size={20} className="text-primary"/><div><p className="text-sm font-semibold">{r.name}</p><p className="text-xs text-muted-foreground">{r.asset_number} · {r.current_project?.name||'Unassigned'} · {r.status}</p></div></button>)}</div></State></Modal>}{asset&&<Assignment assetId={asset} projectId={projectId} onClose={()=>setAsset('')} onSaved={()=>{setAsset('');onSaved();}}/>}</>;}
