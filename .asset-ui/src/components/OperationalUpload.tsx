 'use client';
import {useState} from 'react';import {Upload} from 'lucide-react';import {apiFetch} from '@/lib/api';import {Modal,Row} from './DataUI';
export default function OperationalUpload({path,photo=false,project=false,onClose,onSaved}:{path:string;photo?:boolean;project?:boolean;onClose:()=>void;onSaved:()=>void}){const [file,setFile]=useState<File|null>(null);const [title,setTitle]=useState('');const [description,setDescription]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [uploaded,setUploaded]=useState<Row|null>(null);async function submit(e:React.FormEvent){e.preventDefault();if(!file)return;setBusy(true);setError('');try{let row=uploaded;if(!row){const body=new FormData();body.append('file',file);body.append(photo?'caption':'title',title);if(description)body.append(project?'description':'notes',description);row=await apiFetch<Row>(path,{method:'POST',body});setUploaded(row);}if(photo)await apiFetch('/api/v1/asset-media/'+row.id+'/set-primary',{method:'POST'});onSaved();}catch(e:any){setError((uploaded?'File saved. Could not finish setting the primary photo. ':'')+e.message);}finally{setBusy(false);}}return <Modal name={photo?'Upload asset photo':project?'Add project file':'Upload asset document'} onClose={onClose}><form className="space-y-4" onSubmit={submit}><label className="block text-xs font-semibold">{photo?'Caption':'Title *'}<input className="input-field mt-1" required={!photo} maxLength={200} value={title} onChange={e=>setTitle(e.target.value)}/></label>{!photo&&<label className="block text-xs font-semibold">Description<textarea className="input-field mt-1" maxLength={20000} value={description} onChange={e=>setDescription(e.target.value)}/></label>}<label className="block rounded-lg border-2 border-dashed p-6 text-center"><Upload className="mx-auto text-primary mb-2" size={24}/><span className="block text-sm font-semibold">{photo?'Choose a PNG or JPEG photo':'Choose a file'}</span><input type="file" className="mt-4 text-xs max-w-full" required accept={photo?'.png,.jpg,.jpeg':'.pdf,.doc,.docx,.xls,.xlsx,.txt,.csv,.png,.jpg,.jpeg'} onChange={e=>{setFile(e.target.files?.[0]||null);setUploaded(null);}}/></label>{error&&<p className="text-sm text-red-700" role="alert">{error}</p>}<div className="flex gap-2 justify-end border-t pt-4"><button className="btn-secondary text-xs" type="button" onClick={onClose}>Cancel</button><button disabled={busy||!file} className="btn-primary text-xs">{busy?'Uploading…':uploaded?'Retry':'Upload file'}</button></div></form></Modal>;}
