 'use client';
import {useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {Bell,Search,LogOut} from 'lucide-react';import {useAuth} from './AuthProvider';import {useData,rows} from './DataUI';
export default function Topbar(){const auth=useAuth();const router=useRouter();const [search,setSearch]=useState('');const [resource,setResource]=useState('projects');const [show,setShow]=useState(false);const notifications=useData('/api/v1/hr/notifications');const unread=rows(notifications.data).filter(r=>!r.read_at).length;const sections=[['Projects','projects','projects.read'],['Employees','employees','employees.read_basic'],['Assets','assets','assets.read'],['Inventory','inventory/items','inventory.read']].filter(([, ,p])=>auth.can(p));return <header className="min-h-14 border-b bg-card flex items-center px-4 md:px-6 gap-3 flex-shrink-0"><form className="hidden md:flex flex-1 max-w-xl items-center gap-2" onSubmit={e=>{e.preventDefault();router.push('/workspace/'+(sections.some(([,r])=>r===resource)?resource:sections[0]?.[1]||'projects')+'?search='+encodeURIComponent(search));}}><Search size={16} className="text-muted-foreground"/><select aria-label="Search section" className="text-xs bg-transparent border-0 max-w-28" value={resource} onChange={e=>setResource(e.target.value)}>{sections.map(([name,r])=><option value={r} key={r}>{name}</option>)}</select><input className="text-sm bg-transparent outline-none flex-1 min-w-0" aria-label="Search records" placeholder="Search records…" value={search} onChange={e=>setSearch(e.target.value)}/><button className="text-xs text-primary">Search</button></form><div className="ml-auto flex items-center gap-3"><Link aria-label={'Notifications'+(unread?', '+unread+' unread':'')} href="/workspace/hr/notifications" className="relative p-2 rounded hover:bg-muted"><Bell size={18}/>{unread>0&&<span className="absolute -top-0.5 -right-0.5 rounded-full bg-accent text-white text-[10px] px-1">{unread}</span>}</Link><div className="relative"><button className="flex items-center gap-2 px-2 py-2 rounded hover:bg-muted" onClick={()=>setShow(!show)} aria-expanded={show}><span className="w-7 h-7 gradient-brand rounded-full flex items-center justify-center text-xs font-bold text-white">{auth.user?.first_name?.[0]}{auth.user?.last_name?.[0]}</span><span className="hidden sm:inline text-sm font-medium">{auth.user?.first_name} {auth.user?.last_name}</span></button>{show&&<div className="absolute right-0 top-full mt-2 card shadow-lg p-4 w-64 z-40"><p className="text-sm break-all">{auth.user?.email}</p><p className="text-xs text-muted-foreground mt-1">{auth.access?.roles.join(', ')}</p><button className="btn-secondary mt-4 w-full" onClick={()=>void auth.signOut()}><LogOut size={14}/>Sign out</button></div>}</div></div></header>;}
